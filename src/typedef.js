/**
 * @typedef {Object} rules
 * @property {int} moveUp move the cursor up these many rows
 * @property {int} moveForward move the cursor forward these many columns
 * @property {color} whenColor color of the text in when description
 * @property {color} thenColor color of the text in then description
 */

/**
 * @typedef {when, then, script} descriptionObject
 */

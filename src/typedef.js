/**
 * @typedef When
 * @type {object}
 */

/**
 * @typedef ListBuilder
 * @type {object}
 * @property {(function(*=): List)} list
 * @property {(function(*=): ListItem)} listItem
 * @property {ViewRules} rules
 */
